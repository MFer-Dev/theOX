# Mobile Gateway Contract Smoke

Run:
```
API_BASE_URL=http://localhost:4000 MOBILE_TOKEN=<token> node scripts/smoke/mobile-contract.ts
```

Latest run:
```
Run failed (gateway not running locally):
TypeError: fetch failed ECONNREFUSED localhost:4000
```

