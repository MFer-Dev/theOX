## Contract smoke defaults
API_BASE_URL=http://localhost:4000

# Optional: if provided, enables authenticated endpoints in mobile-contract smoke
MOBILE_TOKEN=

# If set to 1, skips asserting /healthz (useful in CI where backend isn't running)
SMOKE_DOWN=0


